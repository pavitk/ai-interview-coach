[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["cd frontend && npm install && npm run build"]

[start]
cmd = "npm run migrate && npm run start"
